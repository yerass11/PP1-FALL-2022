#include <iostream>
#include <vector>
#include <map>

using namespace std;

vector <int> merge(vector<int> &a, vector <int> &b) {
    
}
int main(){
    int n; cin >> n;
    vector <int> a(n);
    for (int i = 0; i < n; ++i) {
        cin >> a[i];
    }


    int m; cin >> m;
    vector <int> b(m);
    for (int i = 0; i < m; ++i) {
        cin >> b[i];
    }


    return 0;
}