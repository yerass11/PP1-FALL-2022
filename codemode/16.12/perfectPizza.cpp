#include <iostream>
#include <vector>
#include <map>

using namespace std;

int main(){
    int n; cin >> n;
    vector <string> v(n);
    for (int i = 0; i < n; ++i) {
        cin >> v[i];
    }

    map <string, vector <string> > pm;
    int m; cin >> m;
    while (m--) {
        string a, b; cin >> a >> b;
        
    }

    return 0;
}