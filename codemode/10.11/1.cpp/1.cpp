#include <iostreAM>
#include <cmath>
#include <vector>

using namespace std;

int prime(int n) {
    int cnt = 0;
    vector<int> v;
    int x = 2;
    while (cnt < n) {
        for (int i = 2; i < sqrt(x) + 1)
    }
}


int main() {
    int n; cin >> n;
    
    
    return 0;
}
